<% format = local_assigns[:format] %>

<%= form_with(model: account, format: format) do |form| %>
  <% if account.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %><br />
    <%= form.text_field :name, class: "border rounded py-2 px-3" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
