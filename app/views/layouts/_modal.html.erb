<div id="modal" data-modal-target="modal" class="hidden fixed top-0 left-0 flex justify-center items-center w-full h-full bg-gray-900 opacity-50">
  <span class="absolute top-0 left-0 w-full h-full" data-action="click->modal#close touch->modal#close"></span>
  <%= turbo_frame_tag :modal_content, class: "relative z-10 max-w-lg p-10 bg-white rounded-lg", data: { modal_target: "modalContent" } %>
</div>
